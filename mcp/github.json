{
  "name": "github",
  "description": "Allows AI to manage issues, review PRs, and search GitHub repositories",
  "toml_template": [
    "[mcp_servers.github]",
    "command = \"npx\"",
    "args = [\"-y\", \"@modelcontextprotocol/server-github\"]",
    "",
    "[mcp_servers.github.env]",
    "GITHUB_PERSONAL_ACCESS_TOKEN = \"${GITHUB_PERSONAL_ACCESS_TOKEN}\""
  ]
}
